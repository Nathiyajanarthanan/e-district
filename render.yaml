services:
  # 1. Backend Web Service (Flask)
  - type: web
    name: e-district-server
    env: python
    region: singapore
    plan: free
    rootDir: server
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: e-district-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: FRONTEND_URL
        fromService:
          name: e-district-client
          type: static # Changed back to static
          property: url

  # 2. Frontend Static Site (Vite)
  # Stripped down to bare essentials as static sites don't use 'env' or 'plan' fields
  - type: static
    name: e-district-client
    region: singapore
    rootDir: client
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        fromService:
          name: e-district-server
          type: web
          property: url
        suffix: /api

# 3. PostgreSQL Database
databases:
  - name: e-district-db
    region: singapore
    plan: free
